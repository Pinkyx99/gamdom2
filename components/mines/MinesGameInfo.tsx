import React from 'react';

// This component is no longer used in the redesigned Mines game.
// It is kept in the codebase to avoid breaking file imports but should be considered deprecated.
const MinesGameInfo: React.FC = () => {
    return null; 
};

export default MinesGameInfo;
